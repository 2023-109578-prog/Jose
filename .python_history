sad
n
1
>sdasd
print("hello")
input()
input()
name = input("Enter your name: ")
print("Hello", name)
input()
name = input("Enter your name: ")
input()
name = input("Enter your name: ")name = input("Enter your name: ")name = input("Enter your name: ")
print ("hello fucking world")
"""
Created on Fri Feb 27 2026

@author: Jcruz
"""
import scipy.stats as sts
import scipy.stats as sts
import scipy.sts as sts

import scipy.sts as sts
python
import scipy.sts as sts
import scipy.stats as sts
import numpy as np
matplotlib.pyplot as plt
import scipy.stats as sts
import numpy as np
import matplotlib.pyplot as plt
import scipy.stats as sts
import numpy as np
import matplotlib.pyplot as plt
mu = np.linspace(1.65, 1.8, num = 50)
test = np.linspace(0, 2)
uniform_dist = sts.uniform.pdf(mu) + 1
uniform_dist = uniform_dist/uniform_dist.sum()
beta_dist = sts.beta.pdf(mu, 2, 5, loc = 1.65, scale = 0.2)
beta_dist = beta_dist/beta_dist.sum()
plt.plot(mu, beta_dist, label = 'Beta Dist')
plt_plot(mu, uniform_dist, label = 'Uniform Dist')
plt.plot(mu, uniform_dist, label = 'Iniform Dist')
plt.xlabel("Value of $\mu$ in meters")
plt.ylabel("probability density")
plt.legend()
import scipy.stats as sts 
import numpy as np
import matplotlib.pyplot as plt

mu = np.linspace(1.65, 1.8, num =50)
test = np.linspace(0, 2)
uniform_dist = sts.uniform.pdf(mu)

unifor_dist = uniform_dist/uniform_dist.sum()
beta_dist = sts.beta.pdf(mu, 2, 5, loc = 1.65, scale = 0.2) 
beta_dist = beta_dist/beta_dist.sum()
plt.plot(mu, beta_dist, label = 'Beta Dist')
plt.plot(mu, uniform_dist, label = 'Uniform Dist')
plt.xlabel(r"Value of $\mu$ in meters")
plt.ylabel("Probability Density")
plt.legend()

import scipy.stats as sts 
import numpy as np
import matplotlib.pyplot as plt

mu = np.linspace(1.65, 1.8, num =50)
test = np.linspace(0, 2)
uniform_dist = sts.uniform.pdf(mu, loc = 1.65, scale = 0.2)

unifor_dist = uniform_dist/uniform_dist.sum()
beta_dist = sts.beta.pdf(mu, 2, 5, loc = 1.65, scale = 0.2) 
beta_dist = beta_dist/beta_dist.sum()
plt.plot(mu, beta_dist, label = 'Beta Dist')
plt.plot(mu, uniform_dist, label = 'Uniform Dist')
plt.xlabel(r"Value of $\mu$ in meters")
plt.ylabel("Probability Density")
plt.legend()

plt.show()
import scipy.stats as sts
import numpy as np
import matplotlib.pyplot as plt

mu = np.linspace(1.65, 1.8, num =50)
test = np.linspace(0, 2)
uniform_dist = sts.uniform.pdf(mu)

unifor_dist = uniform_dist/uniform_dist.sum()
beta_dist = sts.beta.pdf(mu, 2, 5, loc = 1.65, scale = 0.2)
beta_dist = beta_dist/beta_dist.sum()
plt.plot(mu, beta_dist, label = 'Beta Dist')
plt.plot(mu, uniform_dist, label = 'Uniform Dist')
plt.xlabel(r"Value of $\mu$ in meters")
plt.ylabel("Probability Density")
plt.legend()
plt.show()

